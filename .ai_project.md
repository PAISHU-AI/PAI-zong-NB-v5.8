# Project Memory

## Project Identity

- Name: PAI-Zong-NB
- Purpose: Current global Codex skills pack and zhuluyou distribution package.

## Current Status

- Status: Active
- Current focus: skills distribution hardening for local skill sync, macOS/Windows installation, lifecycle audit, commercial defaults, overlays, and project memory evidence.

## Tech Stack

- Markdown skills and docs.
- Bash installer for macOS.
- PowerShell install/verify scripts for Windows compatibility.
- Node.js scripts inside `skills/zhuluyou/scripts/` for routing, memory governance, and skill lifecycle checks.
- Optional macOS launchd, Windows Task Scheduler, and Git pre-push maintenance setup.

## Key Commands

- `bash scripts/install-macos.sh --force`
- `bash scripts/install-macos.sh --force --setup-maintenance`
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-skills.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/install.ps1 -Force -SetupMaintenance`
- `node scripts/verify-skills.mjs`
- `node skills/zhuluyou/scripts/verify-memory-bootstrap.mjs`
- `node skills/zhuluyou/scripts/audit-memory-system.mjs --cwd .`
- `node skills/zhuluyou/scripts/audit-project-memory.mjs --cwd .`
- `node skills/zhuluyou/scripts/init-project-memory.mjs --cwd . --dry-run`
- `node skills/zhuluyou/scripts/audit-skill-lifecycle.mjs`
- `node scripts/audit-commercial-content.mjs`
- `node scripts/audit-skill-sources.mjs`
- `node scripts/build-commercial-release.mjs --out /tmp/PAI-Zong-NB-commercial --force`

## Project Structure Entry

- See `docs/project-structure.md`.

## Docs Index

- See `docs/INDEX.md`.

## Active Work / Next Actions

- Keep `skills/zhuluyou/` synchronized with the installed global routing and memory system.
- Keep local standard skill directories synchronized into `skills/`; skip empty local directories without `SKILL.md`.
- Keep skills version-aligned with the separately distributed `AGENTS5.8.md`; do not distribute global or project-level `AGENTS.md`.
- Keep project and global memory maintenance setup optional, explicit, and idempotent across macOS and Windows.
- Keep the owner profile free of secrets, tokens, account identifiers, and private conversation history.
- Preserve `PAI-Zong-personal` only as a compatibility overlay for older install workflows.
- Re-run memory, route, lifecycle, commercial-content, and package validation after skill changes.

## Risks / Known Issues

- The repository intentionally excludes global `AGENTS.md`, project-level `AGENTS.md`, `AGENTS5.8.md`, and other system prompts.
- Installing or deleting skills under `$CODEX_HOME/skills` still requires a Codex restart before runtime discovery is guaranteed.
- External or newly added skills must pass lifecycle and poisoning checks before default routing.
- Lifecycle scanner allowlist entries must remain exact-match with rationale; do not use broad suppressions.

## Memory Update Rules

- Update `docs/INDEX.md` when adding durable docs.
- Update `docs/project-structure.md` when package structure changes.
- Update `docs/roadmap.md` for v2.2 rollout tasks, validation gaps, or future memory-system work.
- Update `docs/memory/*` for durable evidence, retrieval index, and invalidations.
